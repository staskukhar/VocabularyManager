# Create a feture for parsing CSV files

## Requirements
- VocabularyCreator page must have UI for mapping fields between model and CSV values
- Each mapping relation must be represented as two combo box elements; model on left, CSV value on right.
- Validation for required fields must be implemented.
- UI styling and tools must comply with ones followed in project
- User must be able to save the words into either new or existing vocabulary