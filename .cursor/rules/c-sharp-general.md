# Rules:
 - Use specific types, no 'var'
 - For simplicity, prefer stright calling instead of reflection when possible
 - Small functions
 - Do not shorten names. Names must be self descriptive. The only exception is lambda expressions.